const toExecute = ref([])

function useMouseHover (fn) {
  // console.log('use mouse hover => ', fn)
  // toExecute.push(fn)
  // useDebounceFn(() => {
  //   console.log('toExecute.length => ', toExecute.length)
  // }, 3000)
}

export default useMouseHover